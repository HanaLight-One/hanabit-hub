# Hanabit Hub Architecture

## 1. 목표

Hanabit Hub는 이미지, 오늘의 운세, 자동화 상태와 향후 작은 도구를 한곳에서
관리하는 Owner Only 웹 허브다.

처음에는 운영 위험과 유지 비용이 낮은 구조로 시작한다. 기능이 커지면 기존
API 계약을 유지하면서 프런트엔드를 React로 전환하고, 백엔드는 모듈 단위로
확장한다.

## 2. 현재 결정

### 애플리케이션과 운영 데이터 분리

- 이 저장소에는 허브의 코드와 문서만 둔다.
- 기존 이미지 저장소, 생성 파이프라인, 예약 작업 및 상태 파일은 이동하지 않는다.
- 외부 경로는 `config.local.json`으로 주입한다.
- 실제 경로가 들어간 로컬 설정은 Git에 커밋하지 않는다.

### 기존 서비스 보존

- 현재 Image Studio의 `127.0.0.1:8787` 운영 서버를 유지한다.
- 새 허브는 개발 중 기본적으로 `127.0.0.1:8790`을 사용한다.
- 검증된 커밋만 실행하는 안정 운영본은 별도 checkout의
  `127.0.0.1:8791`을 사용한다.
- Cloudflare Tunnel, DNS, Access 정책은 승인 전까지 변경하지 않는다.
- 전환은 병행 검증 후 이루어지며 기존 운영 폴더를 즉시 삭제하지 않는다.

### 보안 경계

- 관리자 허브와 공개 콘텐츠 사이트를 분리한다.
- 관리자 API는 Cloudflare Access 뒤에 두더라도 최소 정보만 반환한다.
- API 응답에 절대경로, 로그, 원본 응답, 키, 쿠키 또는 세션을 포함하지 않는다.
- 파일 접근은 설정에 등록된 루트와 안전한 상대경로만 허용한다.
- 서버 제어는 사전에 코드로 정의한 작업의 allowlist만 허용한다.
- 범용 셸, 임의 실행 파일, 임의 인수 또는 임의 경로를 받는 API는 금지한다.
- 삭제와 영구 변경은 명시적 확인 및 활동 기록을 요구한다.
- 긴급 Codex 재기동은 같은 출처의 확인된 요청만 받고 60초 동안 중복 실행을
  잠근다. 설치된 `OpenAI.Codex` 패키지의 프로세스만 대상으로 하며 요청 기록은
  Git에서 제외된 로컬 상태에 남긴다.
- 화풍 관리는 설정된 화풍 루트 안의 `[화풍] 이름.txt`만 새로 추가하며 기존
  파일의 덮어쓰기와 삭제를 허용하지 않는다. 색인 갱신은 설정에 고정된 Python과
  파이프라인의 `build_index.py`만 실행하고 임의 명령·경로·인수를 받지 않는다.
- 추가 생성의 저장 화풍은 최대 3개까지 선택할 수 있다. 허브가 검증된 화풍 본문을
  하나의 혼합 지시로 결정적으로 조립하며, 외부 worker에는 기존 단일 화풍 계약으로 전달한다.
- 추가 생성의 인물·화풍 자동 선택은 초안 ID를 시드로 한 번만 결정한다. worker 시작 전에
  실제 인물 ID·관계 그룹·화풍 ID로 작업 기록을 바꾸어 결과 카드와 DB가 선택값을 설명할 수 있게 한다.
  `등장인물 없음`은 프롬프트에만 존재하는 외부 인물·사물을 위한 명시적 경계로 유지한다.
- 제작 기록이 있는 소스 이미지의 유지 모드는 원본의 인물·화풍 ID를 폼에 명시적으로 복원한다.
  자동 재생성이 아니라 사용자가 복원값을 확인·수정한 뒤, 소스 이미지를 주 참조로 함께 보내는
  새 작업으로 실행한다. 기본 `함께 한 장`은 기존 최대 6명을 한 이미지에 배치한다.
  `인물별 배치`는 최대 10명의 선택 인물을 각각 독립된 한 장으로 고정하고,
  `인물 없는 변주`는 같은 프롬프트·화풍·소스를 2~10개의 독립 이미지 호출로 전달한다.
  배치 전체는 하나의 작업과 영수증을 공유한다. Hub가 시작한 worker만 환경변수로
  최대 동시 실행 4개를 요청하고, 기존 8787 등 환경변수가 없는 실행은 worker 기본값 2를 유지한다.
- 완료·실패 이미지의 `그대로 재생성`은 기존 결과 이미지를 참조하지 않는다. 저장된 원본
  프롬프트, 확정 인물, 확정 화풍과 이미지 앵커 사용 여부만 새 1장 초안으로 복원한다.
  인물별 배치에서는 요청한 슬롯의 한 사람만 복원하며 진행 중 작업은 재생성할 수 없다.
- 뉴스의 제목 태그(`공식`, `사례`, `유추` 등)와 DC 말머리는 별도 필드다. 모델은 제한된
  말머리 후보만 제안하며, 최종 값은 로컬 allowlist 라우터가 `뉴스/소식`, `💡 정보`, `잡담`,
  `AI창작` 중 하나로 확정한다.
- 뉴스 원문 이미지가 없을 때만 Git으로 관리되는 말머리 기본 커버를 게시 단계에서 한 장
  추가한다. 기본 커버는 원문 미디어로 기록하지 않고 고정 파일 allowlist로만 읽는다.

## 3. 목표 정보 구조

```text
/                  홈 대시보드
/images            이미지 아카이브
/images/create     추가 생성과 대기열
/images/styles     화풍 파일 관리와 색인 상태
/images/theme-thumbnails  오늘의 테마 대표 썸네일, 가중치와 날짜별 강제 선택 관리
/images/trash      이미지 휴지통
/dc                개인 DC 원고 편집실
/fortune           오늘의 운세
/automations       자동화 상태
/status            서버 상태
/tools/*           향후 작은 도구
```

홈은 각 기능 전체를 복제하지 않는다. 오늘의 상태, 경고, 최근 항목과
모듈 바로가기만 제공한다.

## 4. 코드 경계

```text
public/             현재의 작은 정적 프런트엔드
src/
  server.mjs        서버 시작점과 HTTP 연결
  config.mjs        외부 설정 로딩과 검증
  modules/          기능별 API와 서비스
  shared/           안전한 공통 유틸리티
test/               API 계약과 안전 경계 테스트
```

각 기능 모듈은 다음 경계를 지킨다.

1. 라우트는 요청과 응답만 처리한다.
2. 서비스는 업무 동작을 처리한다.
3. 저장소 어댑터는 허용된 파일 또는 외부 시스템만 읽는다.
4. 프런트엔드는 공개된 API 계약만 사용하며 로컬 경로를 알지 못한다.

## 5. 단계적 확장

### 단계 0 — 기반

- 독립 저장소와 개발 포트 마련
- 외부 설정과 Git 제외 규칙 마련
- 상태 확인 API 및 정적 시작 화면 제공

### 단계 1 — 기존 Image Studio 연결

- 기존 동작을 테스트로 기록
- 절대경로가 없는 안전한 응답 계약 정의
- 이미지 기능을 `images` 모듈로 연결
- 서울 시간 오전 02시를 운영일 경계로 사용
- 날짜별 테마를 허브 로컬 상태에 보관하고 이미지 날짜와 연결
- 생성 파이프라인이 이미지별 구조화된 제작 기록을 남기게 함
- 직접 업로드 이미지는 `state/image-source-uploads/`에 별도 보관하고, 공개 API에는
  해시 ID와 안전한 이미지 URL만 노출한다. 초안은 새 장면에서도 이 ID를 보존하며 실행 직전에
  아카이브 안의 실제 파일인지 다시 확인한 뒤 Responses worker의 주 참조 이미지로 전달한다.
  파일 선택과 클립보드 붙여넣기는 동일한 형식·크기 검증을 거치며, 삭제는 업로드 파일에만
  허용하고 기존 복원 가능한 이미지 휴지통 경계를 재사용한다.
- 기존 8787 서비스는 그대로 유지

### 단계 2 — 허브 화면

- 홈 대시보드 추가
- 운세, 자동화, 상태를 읽기 전용 모듈로 추가
- 모바일과 데스크톱 내비게이션 추가

### 단계 3 — 운영 전환

- 새 허브를 별도 포트에서 병행 검증
- 개발 8790과 안정 운영 8791을 분리하고 검증된 커밋만 운영본에 반영
- 사용자 승인 후 시작 방식과 Tunnel 원점 전환
- 안정화 기간 후 기존 서버의 보관 여부 결정

### 단계 4 — 공개 사이트

- `fortune.hanabit.one` 같은 별도 읽기 전용 사이트 구성
- 관리자 API를 직접 노출하지 않고 정제된 공개 데이터만 전달

## 6. React 전환 알림 기준

### 2026-08-01 전환 결정

이미지 생성실이 목적 선택, 작업 상태 갱신, 아카이브 필터처럼 여러 화면 상태를 공유하기
시작해 전환 기준에 도달했다. 이번 목적별 분류와 상태판까지는 현재 정적 화면으로 안전하게
완성하고, 다음 UI 기능을 추가하기 전에 React 전환 기반을 먼저 마련한다. 기존 Node API
계약과 8791 운영 경계는 유지하며 프런트엔드만 단계적으로 교체한다.

뉴스 수집·번역·판정은 UI와 독립된 백엔드 모듈로 유지한다. `/news`를 첫 React 파일럿으로
전환해 원문·번역·판정·이미지 검토와 DC 게시 승인 영수증 저장을 구현했다. 승인은
`approved_for_dc` 상태로 원자적으로 기록한다. 수동 게시 기능은 원고 미리보기 뒤에만
노출하며, 실제 게시자는 별도 프로세스 경계와 중복 방지 제출 의도·게시 영수증을 요구한다.
외부 링크는 허용된 호스트의 단독 HTTPS 줄만 앵커로 변환하고, 게시자가 돌려준 모바일 DC 주소는
검증된 글 번호를 포함한 표준 PC 주소로 정규화한 뒤에만 성공 영수증으로 인정한다.
본문 강조 서식도 모델 HTML을 받지 않고 고정 섹션 이름만 공용 렌더러에서 제한된 HTML로 바꾸며,
원문·번역·해설 문자열은 모두 이스케이프한 뒤 게시자 프로세스에 전달한다.
외부 원문 링크는 이미지 첨부 뒤 본문의 첫 섹션에 두어 플랫폼 임베드가 번역보다 먼저 보이게 한다.

일반 DC 편집실은 텍스트와 이미지를 순서가 있는 블록으로 저장한다. 기존 초안은
이미지 상단 첨부와 본문 한 블록으로 호환 복구하고, 새 초안은 게시 직전 격리된 이미지의
인덱스와 블록 순서를 함께 해시한다. 게시 라이브러리에는 고정 인라인 이미지 표식만
전달하며 표식 누락·중복·범위 이탈은 실제 게시 전에 거부한다. DC 첨부 상한에 맞춰
이미지는 최대 50장, 이미지 사이의 텍스트를 포함한 본문은 최대 101블록으로 제한한다.
뉴스 자동 게시는 시작 영수증 이후 처리된 항목 중 번역 귀속·출처·가치·중복·15분 간격 관문을
모두 통과한 단건만 수동 게시와 동일한 실행 경계로 제출한다. 숫자 글 번호와 표준 URL을 모두
확인하지 못한 결과는 모호한 최종 상태로 남기고 자동 재시도하지 않는다.

뉴스 AI는 계단식 비용 경계로 운영한다. 무료 텍스트 분석이 번역과 1차 판정을 맡고,
결정적 코드가 정한 애매함 조건에 해당하는 X 항목만 일일 상한 안에서 Codex
`exec` 심층검토로 전달한다. Codex는 일회성·읽기 전용·구조화 출력으로 실행하며
이미지 픽셀, 비밀값, 게시 권한을 받지 않는다. 모델 판정만으로는 게시할 수 없고, 로컬의 번역
귀속·출처·중복·간격·DC 사전검사 관문이 모두 통과된 경우에만 감시기가 자동 제출한다. 그 외
항목은 사람이 원고 미리보기를 연 뒤 별도의 수동 게시 확인을 해야 한다.

Responses API 공용 텍스트 실행 코드의 정본은 `tools/openai-free/`에서 Git으로
관리한다. 개발·운영 checkout은 자기 정본을 직접 실행하고, 컴퓨터에 종속된 Python
가상환경과 DPAPI 키 저장소 경로만 Git 제외 설정으로 주입한다. 이미지 worker에도 같은
두 경계를 환경변수로 전달한다. 키 내용, 암호문, 프롬프트와 응답은 정본에 포함하지 않는다.

뉴스의 게시 가치와 정보 확실성은 분리한다. `[유추]`도 구체적이고 신뢰 가능한 초기
신호면 게시 후보가 될 수 있다. 자동 게이트는 태그·출처 등급·표현 제한을 결정론적으로
재검사하고 게시 영수증을 남긴 뒤에만 외부 게시를 허용한다.

자동 편집장은 최근 24시간 사건 중복, 대표 원고 점수, 15분 연속 게시 경쟁과 `[유추]`
심층검토 여부를 계산한다. 자동 게시 시작 이전 항목은 중복 비교에만 사용하고 소급 제출하지 않으며,
`ready`가 아닌 결과는 운영 상태를 게시 완료로 변경하지 않는다.
무료 번역은 별도 로컬 원문 경계 감사가 URL 제거, 원문·관련 글 분리와 원문에 없는 불변 식별자의
추가 여부를 결정론적으로 재검사한다. 이 감사는 의미 검증을 대신하지 않으므로 공식·확정·사례에만
자동 후보 자격을 줄 수 있고, 유추는 반드시 제한된 Codex 심층검토를 추가로 통과해야 한다.

`/notifications`는 두 번째 React 모듈이며 표준 Web Push만 사용한다. VAPID 개인키와
기기별 Push 구독은 Git 제외 상태에 보관하고 브라우저에는 공개키만 제공한다. 알림 종류는
코드 allowlist로 제한하며 임의 제목·본문·URL을 받는 발송 API를 만들지 않는다. 외부
파이프라인 알림은 각 기능의 완료 영수증을 확인하는 경계에서 단계적으로 연결한다.

무료 공식 뉴스 레인은 X보다 우선하는 확정 출처다. 공식 GitHub 안정 릴리스와 OpenAI가 관리하는
제품·API 변경 기록을 15분 간격으로 확인한다. 소스별 첫 관측은 기준선 영수증만 남겨 과거 항목의
대량 유입을 막고, 이후 새 항목만 기존 번역·중복 묶음·게시 관문에 합류시킨다. 파일과 이미지는
수집하지 않으며 공개 원문·링크·릴리스 메타데이터만 뉴스 대기함에 저장한다.

외신 RSS는 확정 출처와 분리된 그림자 레인이다. 허용된 The Register·Ars Technica 피드에서
제목·짧은 설명·시각·링크 메타데이터만 보관하고 기사 HTML·전문·이미지를 가져오지 않는다.
새 항목은 `shadow_radar`로 허브에만 표시하며 번역·알림·게시 함수를 호출하지 않는다. 향후
1차 출처 탐색과 교차 확인이 검증되기 전에는 외신만을 근거로 자동 원고를 만들지 않는다.

X 새 글 감지는 공식 Filtered Stream 어댑터가 담당하고, 감지 결과를 기존 Discord
`#x-watch` 수집 경계로 되돌려 중복 방지·번역·판정 로직을 재사용한다. 이 어댑터는
사용량 과금 동의와 별도 비밀값이 모두 준비되기 전까지 기본 비활성으로 유지한다.

아래 조건 중 두 개 이상이 충족되거나 하나가 개발을 뚜렷하게 방해하면 새 기능
추가를 잠시 멈추고 React 전환을 제안한다.

- 주요 화면이 5개 이상이다.
- 여러 화면이 동일한 복잡한 상태를 공유한다.
- 로그인, 역할 또는 세밀한 권한 UI가 필요하다.
- 실시간 상태 갱신과 상호작용이 복잡해진다.
- 같은 DOM 렌더링 또는 이벤트 코드가 반복된다.
- 단일 프런트엔드 파일이 약 500줄을 넘는다.
- UI 회귀 테스트와 컴포넌트 단위 검증의 필요성이 커진다.

React 전환 전에도 API와 화면을 분리한다. 따라서 전환 시 백엔드나 기존
파이프라인을 다시 작성하지 않고 프런트엔드만 단계적으로 교체할 수 있어야 한다.

## 7. 백엔드 확장 알림 기준

현재 Node.js 표준 서버로 충분한 동안은 프레임워크를 추가하지 않는다. 다음
상황에서는 Fastify 등의 도입이나 별도 백엔드 구조를 검토한다.

- 라우트와 미들웨어가 반복되어 안전하게 관리하기 어렵다.
- 데이터베이스, 사용자 인증 또는 외부 서비스 연동이 필요하다.
- 백그라운드 작업과 웹 요청의 수명주기를 분리해야 한다.
- API 스키마 검증과 관측성이 운영상 필수가 된다.

### 2026-08-01 SQLite 원장 기반

교차 출처 뉴스 묶음, 승인과 게시 영수증에 관계·고유성·트랜잭션이 필요해져 SQLite
도입 조건에 도달했다. `state/hanabit-hub.sqlite`에는 뉴스 메타데이터만 저장하고 기존
JSON 대기함과 외부 이미지·운세 파일은 이동하지 않는다. 스키마는 버전 마이그레이션으로
관리하며, 운영 전환은 별도 단계에서 기존 JSON 보존과 검증 후 수행한다.

이미지 파일은 기존 폴더에 유지하고 SQLite 스키마 2의 `image_assets`와
`image_generation_metadata`에는 상대 저장 키와 제작 메타데이터만 색인한다.
Hub 완료 작업의 프롬프트·인물·화풍·이미지 앵커 선택을 결과 이미지 ID와 연결하며,
아카이브 밖 경로는 색인하지 않는다. 프롬프트는 Owner Only 이미지 상세 API에서만
제공하고 공개 사이트 계약에서는 제외한다.

## 8. 변경 승인 필요 항목

다음 변경은 구현 전에 범위와 복구 방법을 설명하고 승인을 받는다.

- Cloudflare Tunnel, DNS, Access 또는 Pages/Sites 배포
- 기존 8787 운영 서버의 중지나 교체
- Windows 예약 작업 및 자동 시작 변경
- 기존 저장소, 상태 파일 또는 파이프라인 위치 변경
- 실제 DCInside 게시 또는 외부 공개
- 데이터의 대량 이동이나 영구 삭제
