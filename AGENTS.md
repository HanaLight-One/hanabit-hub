# Hanabit Hub 개발 규칙

이 문서는 이 저장소에서 작업하는 모든 사람과 Codex 에이전트가 따라야 하는
프로젝트 규칙이다. 더 구체적인 사용자 지시가 있으면 해당 지시를 우선한다.

## 1. 기본 원칙

- 작게 시작하고 기능 경계를 분명하게 유지한다.
- 기존 Image Studio, 이미지 저장소, 생성 파이프라인, 예약 작업을 이동하거나
  임의로 변경하지 않는다.
- 새 기능을 기존 단일 파일에 계속 붙이지 않고 기능별 모듈로 분리한다.
- 현재 결정과 전환 기준은 `ARCHITECTURE.md`를 따른다.
- 기존 사용자 변경사항과 운영 데이터를 보존한다.

## 2. 작업 범위와 승인

다음 작업은 실행 전에 사용자에게 변경 범위와 복구 방법을 설명하고 승인을 받는다.

- Cloudflare Tunnel, DNS, Access 및 배포 설정 변경
- 기존 8787 운영 서버의 중지, 교체 또는 시작 방식 변경
- Windows 예약 작업 및 자동 시작 변경
- 기존 저장소, 파이프라인 또는 상태 파일의 이동
- 실제 DCInside 게시 또는 외부 공개
- 대량 이동, 영구 삭제 및 복구하기 어려운 변경

개발 서버는 기본적으로 `127.0.0.1:8790`을 사용한다. 다른 포트를 사용해야
한다면 먼저 현재 사용 여부와 서비스 정체를 확인한다.

## 3. 보안

- `.env`, 키, 토큰, 쿠키, 세션 및 브라우저 저장소를 읽거나 출력하지 않는다.
- `config.local.json`, 상태 파일, 로그, 생성 파일 및 실제 운영 경로를 커밋하지 않는다.
- API 응답에 절대경로, 내부 로그, 원본 외부 응답 또는 인증 정보를 포함하지 않는다.
- 파일 접근은 설정에 등록된 루트와 검증된 상대경로로 제한한다.
- 서버 제어는 코드에 명시된 allowlist 작업만 허용한다.
- 임의 셸 명령, 실행 파일, 인수 또는 파일 경로를 받는 API를 만들지 않는다.
- 공개 사이트는 관리자 허브 및 관리자 API와 분리하고 읽기 전용으로 구성한다.

## 4. 코드 구조

- HTTP 라우트는 요청 검증과 안전한 응답 변환만 담당한다.
- 업무 동작은 기능별 서비스에 둔다.
- 파일 및 외부 시스템 접근은 별도 어댑터에 둔다.
- 프런트엔드는 API 계약만 사용하고 로컬 파일 경로를 알지 못하게 한다.
- 공통화는 실제 중복이 생긴 뒤 수행하며 추측성 프레임워크를 미리 추가하지 않는다.
- 새 의존성은 표준 기능으로 충분하지 않을 때만 추가하고 이유를 기록한다.

## 5. React와 백엔드 확장

- 현재는 작은 정적 프런트엔드를 유지한다.
- `ARCHITECTURE.md`의 React 전환 조건이 충족되기 시작하면 새 화면을 계속
  추가하기 전에 사용자에게 전환 시점임을 알린다.
- React 전환 전에도 API와 화면의 경계를 유지하여 프런트엔드만 교체할 수 있게 한다.
- Node 표준 서버가 충분한 동안은 백엔드 프레임워크를 추가하지 않는다.

## 6. 검증

- 변경 전 관련 코드와 현재 Git 상태를 확인한다.
- 기능 변경 후 `npm.cmd run check`를 실행한다.
- API 변경 시 성공, 잘못된 입력 및 경로 이탈 방지 동작을 테스트한다.
- 위험 기능은 실패 시 기존 데이터가 보존되는지도 검증한다.
- 기존 8787 서버나 실제 외부 게시를 검증용으로 실행하지 않는다.
- 실패한 검증이나 확인하지 못한 항목을 완료된 것처럼 보고하지 않는다.

## 7. Git 작업

- 하나의 커밋에는 하나의 기능 또는 하나의 명확한 목적만 담는다.
- 리팩터링과 기능 변경은 가능하면 별도 커밋으로 나눈다.
- 관련 없는 사용자 변경사항을 커밋에 포함하지 않는다.
- 커밋 전 `git diff`와 `git status`로 범위를 확인한다.
- 커밋 전 관련 검증을 실행한다.
- 커밋 메시지는 짧고 구체적인 한글 명령형으로 작성한다.
- 완료된 기능 단위는 관련 검증 후 에이전트가 적절한 시점에 커밋하고 푸시할 수 있다.
- 커밋이나 푸시 전에 관련 없는 사용자 변경사항이 섞이지 않았는지 반드시 확인한다.
- Pull Request 생성, 강제 푸시, 기록 재작성은 별도 사용자 승인을 받는다.

권장 커밋 메시지 예시:

```text
허브 기초 서버와 상태 API 추가
이미지 목록 API를 안전한 응답 형식으로 분리
모바일 하단 내비게이션 추가
절대경로 노출 회귀 테스트 추가
```

다음과 같은 모호한 메시지는 피한다.

```text
수정
업데이트
작업함
여러 변경
```

## 8. 문서화

- 작업을 시작할 때 `PROJECT_MAP.md`에서 현재 프론트·백엔드·DB·운영 경계를 확인한다.
- 화면 URL, 기능 모듈, DB 스키마, 상태 저장 위치, 실행 진입점 또는 외부 시스템
  경계가 바뀌면 같은 커밋에서 `PROJECT_MAP.md`를 갱신한다.
- 새로운 설정 항목은 실제 값 없이 `config.example.json`에도 추가한다.
- 실행 방법이나 요구 버전이 바뀌면 `README.md`를 갱신한다.
- 구조, 보안 경계 또는 기술 전환 기준이 바뀌면 `ARCHITECTURE.md`를 갱신한다.
- 컴퓨터별 임시 메모가 필요하면 Git에서 제외되는 `AGENTS.local.md`를 사용한다.

## 9. 서브에이전트 위임

- 메인 에이전트는 조사 결과, 작업을 독립된 하위 과제로 나누는 것이 속도나 품질을
  실질적으로 높인다고 판단하면 사용자의 추가 확인 없이 서브에이전트를 사용할 수 있다.
- 위임을 시작할 때는 어떤 하위 과제를 왜 나누는지 사용자에게 짧게 알린다.
- 메인 에이전트는 요구사항 해석, 전체 설계, 우선순위, 변경 범위, 최종 통합과 검증을
  직접 책임진다. 서브에이전트의 결론은 참고자료이며 검증 없이 그대로 채택하지 않는다.
- 병렬화는 저장소 탐색, 문서 조사, 테스트 범위 조사, 로그 분류처럼 서로 독립적인
  읽기 중심 작업을 우선한다. 같은 파일이나 상태를 여러 에이전트가 동시에 수정하게
  하지 않는다.
- 작고 순차적인 작업이나 조정 비용이 더 큰 작업은 메인 에이전트가 직접 처리한다.

### 모델별 안전 경계

- `gpt-5.6-luna`는 명확하고 반복적인 목록화, 형식 확인, 제한된 읽기 전용 검색처럼
  결과를 메인 에이전트가 쉽게 재검증할 수 있는 보조 작업에만 사용한다.
- 코드 리뷰, 보안 검토, 아키텍처 결정, 데이터 마이그레이션, 인증·비밀값 경계,
  동시성·중복 게시 판단, 삭제·복구, 배포와 운영 변경처럼 놓치면 안 되는 작업은
  `gpt-5.6-luna`에 맡기지 않는다.
- 중요한 검토와 모호한 다단계 판단은 메인 `gpt-5.6` 또는 그에 준하는 고성능
  검토 에이전트가 수행한다. 빠른 탐색용 에이전트의 결과도 최종 반영 전에 메인이
  관련 코드와 테스트로 다시 확인한다.
- 서브에이전트 수와 범위는 필요한 만큼만 사용한다. 병렬화로 토큰·충돌·검토 비용이
  커질 가능성이 있으면 단일 에이전트 작업을 우선한다.
