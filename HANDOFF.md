# Hanabit Hub 세션 인계

이 문서는 새 작업 세션이 빠르게 현재 위치를 찾기 위한 시점별 스냅샷이다.
저장소 문서, 현재 코드와 Git 기록이 언제나 최종 정본이며, 아래 상태는 작업을 시작하기
전에 읽기 전용으로 다시 확인한다.

## 마지막 확인

- 확인일: 2026-08-04 (Asia/Seoul)
- 개발 checkout: `hanabit-hub`, `main` 브랜치, 작업 트리 깨끗함
- 안정 운영 checkout: `hanabit-hub-live`, detached checkout, 작업 트리 깨끗함
- 안정 운영 및 마지막 검증 기능 커밋: `718fab3` — `뉴스 덧붙임 말을 본문 끝으로 이동`
- 개발·원격의 현재 문서 커밋은 이 파일에 고정하지 않고 `git log -1`로 확인한다.
- 마지막 전체 검사: 354개 통과

## 실행 역할

- 기존 Image Studio: `127.0.0.1:8787`
- Hub 개발본: `127.0.0.1:8790`
- Hub 안정 운영본: `127.0.0.1:8791`
- 마지막 확인 시 세 서비스의 health 응답은 모두 HTTP 200이었다.
- 뉴스 감시기는 Windows 작업 `Hanabit News Discord Watcher`로 운영한다.
- `studio.hanabit.one`은 Owner Only 관리자 허브다.

## 최근 완료 내용

- 뉴스 카드에서 DC 게시 원고를 미리 볼 수 있다.
- 선택적인 `게시글에 덧붙일 말` 입력은 수동 DC 게시 버튼 바로 위에 있다.
- 입력한 말은 별도 표찰 없이 실제 게시글의 마지막 문단에 들어간다.
- 입력값이 비어 있으면 게시글에 아무 흔적도 남지 않는다.
- 뉴스 화면은 버전 주소를 사용해 모바일의 오래된 캐시를 피한다.
- `@btibor91` Tibor Blaho를 제품 변화 관찰 후보로 개발 명부에 추가했다. 공식 출처로
  취급하지 않으며 자동 게시하지 않는다. 실제 X 스트림 규칙은 아직 동기화하지 않았다.
- 제품 실무 인물의 짧은 X 답글이 부모 글의 키워드로 수집되지 않도록 원격 규칙에서
  답글을 제외한다.

## 현재 미완료 작업

- 없음.

## 새 세션 시작 순서

1. `AGENTS.md`, `PROJECT_MAP.md`, `ARCHITECTURE.md`, `README.md`를 순서대로 읽는다.
2. 개발 checkout의 `git status`와 최근 커밋을 확인한다.
3. 8787, 8790, 8791의 리스닝 상태와 health 응답을 읽기 전용으로 확인한다.
4. 안정 운영 checkout의 작업 트리와 커밋을 확인한다.
5. 이 문서의 스냅샷과 차이가 있으면 실제 저장소와 실행 상태를 우선한다.

## 반드시 지킬 운영 경계

- `.env`, 키, 토큰, 쿠키, 세션과 DPAPI 저장 내용을 읽거나 출력하지 않는다.
- 실제 DC 게시 테스트는 사용자가 명시한 특정 건에만 한 번 수행한다.
- 게시 결과가 불명확하면 자동 재시도하지 않는다.
- Cloudflare, DNS, Tunnel과 Windows 예약 작업 변경은 범위와 복구 방법을 먼저 설명하고
  승인받는다.
- 기존 8787, 이미지 저장소, 외부 Python 경로, 예약 작업과 사용자 변경사항을 보존한다.
- 기능 변경 후 `npm.cmd run check`를 실행한다.
- 완료된 기능은 구체적인 한글 커밋으로 남기고 푸시한다.
- 운영 반영 시 검증된 커밋만 안정 checkout에 적용하고 8791만 안전하게 재기동한다.
  운영 배포 과정에서 8787과 8790을 중지하지 않는다.
